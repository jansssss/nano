services:
  - type: web
    name: nano-budget-dashboard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn web_dashboard:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
